[1mdiff --git a/lib/src/features/calendar/presentation/widgets/event_list_item.dart b/lib/src/features/calendar/presentation/widgets/event_list_item.dart[m
[1mindex 7bdc693..b13d5ce 100644[m
[1m--- a/lib/src/features/calendar/presentation/widgets/event_list_item.dart[m
[1m+++ b/lib/src/features/calendar/presentation/widgets/event_list_item.dart[m
[36m@@ -165,29 +165,15 @@[m [mclass EventListItem extends ConsumerWidget {[m
                         value: event.isCompleted,[m
                         shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(4)),[m
                         activeColor: categoryColor,[m
[31m-                        onChanged: (val) async {[m
[32m+[m[32m                        onChanged: (val) async {////增加完成日程选项[m
                           final repo = ref.read(eventRepositoryProvider);[m
                           final isar = await repo.db;[m
                           final master = await isar.eventModels.get(event.id);[m
[31m-                          if (master == null) return;[m
[31m-[m
[31m-                          final dateStr = DateFormat('yyyy-MM-dd').format(event.startTime);[m
[31m-[m
[31m-                          if (master.recurrenceRule != null || master.lunarRecurrence != null) {[m
[31m-                            // 重复事件：更新 completedDates 列表[m
[31m-                            final dates = master.completedDates?.split(',').where((s) => s.isNotEmpty).toList() ?? [];[m
[31m-                            if (val == true) {[m
[31m-                              if (!dates.contains(dateStr)) dates.add(dateStr);[m
[31m-                            } else {[m
[31m-                              dates.remove(dateStr);[m
[31m-                            }[m
[31m-                            master.completedDates = dates.isEmpty ? null : dates.join(',');[m
[31m-                          } else {[m
[31m-                            // 普通事件：直接修改 isCompleted[m
[32m+[m[32m                          if (master != null) {[m
                             master.isCompleted = val ?? false;[m
[32m+[m[32m                            await repo.updateEvent(master);[m
[32m+[m[32m                            onCompletionChanged?.call();[m
                           }[m
[31m-                          await repo.updateEvent(master);[m
[31m-                          onCompletionChanged?.call();[m
                         },//已完成日程选项。[m
                       ),[m
                     ),[m
